

const Home = (language)  => {
  

  return (`<div id="home" class="home sections">
    <h1 class="title-home">${language.home.title}</h1>
    <p class="title-two-home">${language.home.content}</p>
  </div>`)
}
