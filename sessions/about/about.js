const About = (language) => {
  return (`<div id="about" class="sections about">
    <h1 class="title-about">${language.about.title}</h1>
    <p class="p-about">${language.about.content}</p>
  </div>`)};
